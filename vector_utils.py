from llm_utils import get_embedding
from typing import List

# CONFIGURE VECTORDB

def store_embeddings(embeddings:List) -> None:
    # Store a list of embeddings in the vector db
    pass

def similiarty_search(query: str) -> List:
    pass
